# 开发

在独立克隆中修改，运行 `python -m pytest tests vendor/tests -q`。测试使用合成数据、Fake/Mock 或本机测试接收端；不得为了回归自动派发真实 Plus 模型任务。

提交前运行 `python tools/release_check.py`。不要提交 state、jobs、真实配置或测试日志。发布包应通过 `python tools/build_release.py` 生成，不能直接压缩整个工作目录。

涉及执行、授权、幂等或用量修改时添加正反测试：允许的操作成功、禁止的操作拒绝，失败保留真实用量，重复事件不重复累计。保留默认失败关闭行为；不能将失败字段直接改为通过。

每次真实模型验收必须单独确认范围，只执行一次；只有网页实际读取结果后才能记录网页闭环通过。
