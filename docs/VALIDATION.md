# 验证范围

0.2.1 安装扩展：2026-09-21 非模型回归 **142 passed, 1 skipped**。新增固定包/运行入口授权、拒绝注入/版本范围、真实合成子进程安装、失败用量保留、超时占用、防重及 DSH 参数测试。另用真实 Node/DSH 入口和模拟 pnpm 执行了隔离 profile 的转发验证；包管理器是模拟的，没有联网安装真实插件。原本机实现针对性回归 **29 passed, 1 skipped**，原 OAuth 连接实际读到了新增项目与安装能力。真实 DSH 插件安装、插件激活和完整网页模型闭环仍未执行。

发布版本 0.2.0 基于已经运行的 PCC 控制器整理，不包含参考部署的账号、运行数据或证据正文。

参考部署已完成：Windows 本机可信模式的一次真实 Plus 合成任务；OAuth-only 连接修复；ChatGPT 发现 7 个 MCP 工具并真实调用 pcc_capabilities。参考部署的完整网页派单/结果/用量/独立审查链仍未完成，因此不作为发布版本的保证。

2026-09-21：在独立虚拟环境安装锁定依赖成功；Windows / Python 3.12 非模型回归为 **131 passed, 1 skipped**（50.28 秒）。跳过项是普通用户缺少符号链接创建权限的测试，不能据此宣称符号链接硬隔离通过；已有 junction 测试仍运行。测试覆盖控制器幂等、项目授权、路径冲突、合成读写/回收删除/依赖/上传下载、失败用量、重复事件、额度窗口和 JWT 验证，另增加可移植运行路径与发布排除检查。

发布白名单与模式扫描通过；这不等同于专业安全审计。默认 doctor 确认未绑定个人运行配置，本轮模型请求为 0。插件及技能结构另外通过官方本地校验脚本检查。

测试不会为收集统计派发 Plus 模型请求。任何新部署都必须使用自己的官方账号完成一次独立验收。硬隔离没有通过保证；Full Access 模式明确不提供严格沙箱。
